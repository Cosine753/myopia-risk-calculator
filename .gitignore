# local-only helpers: reference non-public participant data paths
_local/

# Defensive: this repository must never contain participant-level records.
# The deployed page embeds fitted model parameters only. These patterns keep a
# stray copy of the research build's data out of a push.
data/
*.csv
heldout*
*_cases.json

# Private analysis materials: keep research code and source data out of the
# public calculator repository.
07_SOURCE_DATA/
08_ANALYSIS_CODE/
*.py
*.ipynb
*.r
*.R
*.xlsx
*.xls
*.parquet
*.pkl
*.rds
*.sav
*.dta
